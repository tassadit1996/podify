import {SafeAreaView} from 'react-native';
import React from 'react';
import colors from './src/utils/colors';

const App = () => {
  return <SafeAreaView style={{backgroundColor: colors.PRIMARY, flex: 1}} />;
};

export default App;
